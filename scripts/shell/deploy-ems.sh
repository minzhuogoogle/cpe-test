#!/bin/basha
mkdir -p terraform
    2  cd terraform
    3  wget https://releases.hashicorp.com/terraform/0.11.8/terraform_0.11.8_linux_amd64.zip
    4  unzip terraform_0.11.8_linux_amd64.zip
    5  sudo apt-get -y install zip
    6  apt-get -y install zip
    7  sudo apt-get -y install zip
    8  unzip terraform_0.11.8_linux_amd64.zip
    9  cp terraform /usr/local/bin/.
   10  cd ~
   11  mkdir elastifile
   12  cd elastifile/
   13  git clone https://github.com/66fastback/Terraform-Elastifile-GCP.git
   14  cd Terraform-Elastifile-GCP/
   15  echo changeme > password.txt
   16  cat password.txt
   17  cp /terraform.tfvars .
   18  ls -altr
   19  terraform init
   20  terraform apply -auto-approve
   21  ls -altr
   22  terraform apply
   23  cd ..
   24  pwd
   25  cd
   26  rm -rf elastifile/
   27  ls -altr
   28  git clone https://github.com/66fastback/Terraform-Elastifile-GCP.git
   29  ls -altr
   30  cd Terraform-Elastifile-GCP/
   31  ls a-ltr
   32  sl a-ltr
   33  ls -altr
   34  vi README.md
   35  cat README.md
   36  apt-get install vim
   37  ls -altr
   38  vi terraform.tfvars
   39  gsutil ls
   40  gsutil ls gs://storage-performance-data/
   41  gsutil ls
   42  gsutil ls gs://cpe-performance-storage
   43  gsutil cp gs://cpe-performance-storage/cpe-performance-storage-b13c1a7348ad.json elastifile.json
   44  ls -altr
   45  vi terraform.tfvars
   46  terraform init
   47  terraform apply
   48  gcloud auth list
   49  vi terraform.tfvars
   50  terraform apply
/apt
51  vi terraform.tfvars
   52  terraform apply -auto-approve
   53  apt install curl
   54  apt-get -y install wget
   55  apt-get -y purge zip
   56  apt-get -y install zip
   57  terraform apply -auto-approve
   58  echo "changeme" > password.txt
   59  ls -altr
   60  vi terraform.tfvars
   61  cat password.txt
   62  terraform apply -auto-approve
root@elastifile-box-2:~/Terraform-Elastifile-GCP#

